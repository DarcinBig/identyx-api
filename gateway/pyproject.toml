[project]
name = "gateway"
version = "0.1.0"
description = "Identyx — API Gateway"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pydantic-settings>=2.14.1",
    "redis>=7.4.0",
]
