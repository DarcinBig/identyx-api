[project]
name = "token-services"
version = "0.1.0"
description = "Identyx — Token Service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "python-jose[cryptography]>=3.5.0",
    "redis>=7.4.0",
]
